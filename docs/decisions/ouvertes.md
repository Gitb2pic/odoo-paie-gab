# Décisions ouvertes

| # | Sujet | Options | Recommandation | Décision d’Alex |
|---|---|---|---|---|
| D-01 | Le prompt 01 annonce « 10 points » du fichier `05` §7, qui n'en contient que 8 (`06` §6 dit aussi 8) | a) traiter 8 + 11-14 = 12 points ; b) compléter à 10 avec les 🔒 restants | b) 9 = `hr.salary.attachment` et gains (ADR-16), 10 = vue formulaire des règles et groupes `hr_payroll` | **Recommandation acceptée** (« go et tes recommandations », 24/09/2026) |
| D-02 | ADR-16 et ADR-17 cités par `CLAUDE.md` §3 (règles 6 et 7) mais absents de `00_INDEX` et de `docs/` | a) les rédiger d'après les règles d'or ; b) attendre un texte d'Alex | a), au statut « proposé » | **Recommandation acceptée** (24/09/2026) → `docs/adr/ADR-16-*.md`, `ADR-17-*.md` proposés, à valider sur le fond |
| D-03 | Versionner `docs/sources_pdf/` (36 Mo) ? | a) versionner ; b) `.gitignore` | b) (résumé par la base de connaissance, CLAUDE.md §9) | **Recommandation acceptée** (24/09/2026) |
| D-04 | Le service `odoo19` (qui sert la base `odoo19` créée par Alex) n'a pas de `db_name` : ses threads cron parcourent **toutes** les bases du rôle `ubuntu`, y compris les bases de test `test_ga_*` (connexions constatées pendant le sprint 0, `C/odoo/service/server.py:99-100`, `C/odoo/service/db.py:444-456`). Risques : crons du service exécutés sur les bases de test pendant les tests (résultats instables), mémoire du service, `dropdb` bloqué | a) ajouter `db_name = odoo19` dans `/home/ubuntu/odoo/odoo/debian/odoo.conf` puis `sudo systemctl restart odoo19` ; b) ne rien changer (le Makefile force déjà `dropdb --force`) | a) — modification de la configuration du service : **je ne la fais pas sans ton accord** | ouvert |
| D-05 | Point 8 (schéma `hr_payroll_gb` du client) non vérifiable sur le VPS | a) dump anonymisé des tables `hr_payroll_gb` ; b) `pg_dump --schema-only` + quelques lignes fictives | a) ou b), à fournir **avant l'étape 6** seulement | ouvert (non bloquant avant l'étape 6) |
| D-06 | Date de référence des paramètres : `_rule_parameter` lit à `date_to` du bulletin ; certains taux (FNH 3 % au 17/07/2026) pourraient dépendre de la date de paiement | a) `date_to` partout ; b) `reference_date` = date de paiement pour les paramètres concernés (option société ou paramètre) | à trancher au plan de l'étape 2.2 à la lecture du fichier `09` de la base | ouvert (étape 2.2) |
| D-07 | Code de la V1 `l10n_ga_dgi_edi` (gabarits `.xlsm`, 11 tests, scripts de migration) absent du VPS | a) le copier dans un dossier hors `addons_path` (ex. `/home/ubuntu/odoo/v1_l10n_ga_dgi_edi`) ; b) attendre | a), **avant l'étape 4.4** (gabarits `.xlsm`) | ouvert (non bloquant avant l'étape 4) |
| D-08 | ADR-18 (check.issue dans la paie) et ADR-19 (points d'accroche Enterprise 19) | valider / amender | valider ; j'amende ensuite `04`, `05`, `06` et `02` en conséquence | ouvert |
| D-09 | Usage de la base `odoo19` (créée par Alex) | a) intouchable, tests uniquement sur `test_ga_*` (règle CLAUDE.md §5) ; b) base de démonstration où installer les modules Gabon au fil des étapes (commande dédiée à ajouter au `Makefile`, qui exclurait `odoo19` du garde-fou `test_ga_*`) | a) par défaut ; b) seulement sur ton instruction explicite | ouvert |
